# AnimeApp (アニレフ) - Claude Instructions

## プロジェクト概要
- サイト名: アニレフ（aniref.net）
- 構成: Next.js App Router + Supabase + Tailwind CSS
- 目的: キャラクターからアニメを探せる情報サイト

---

## セッション開始時に必ずやること

1. `C:\Users\kouku\.claude\projects\C--Users-kouku-AnimeApp\memory\MEMORY.md` を読み込み、前回の作業内容・ルール・未着手タスクを把握してから作業を始める

---

## セッション終了時・作業の区切りに必ずやること

作業の区切り（機能追加・バグ修正・スクリプト実行・SQL生成など）ごとに、以下のルールに従ってメモを残す。

### メモの書き込みルール

#### 書くべき場面と種類

| 種類 | 書くタイミング | 例 |
|------|--------------|-----|
| `project` | 作業が一区切りついた / SQLを生成・適用した / 次回持ち越しが生まれた | 翻訳SQL生成、機能追加完了 |
| `feedback` | ミス・トラブルが起きた / 「次回からこうして」と指示された | API失敗の教訓、ルール変更 |
| `reference` | 外部リソースの場所を知った | Supabaseのテーブル構成、外部API |
| `user` | ユーザーの好み・スキル・方針が明確になった | 技術レベル、判断基準 |

#### 書かなくていいもの
- コードの中身・ファイルパス（コードを読めばわかる）
- gitのcommit履歴で十分な変更内容
- 今回限りの一時的な作業

### メモの保存場所
- ファイル: `C:\Users\kouku\.claude\projects\C--Users-kouku-AnimeApp\memory\{種類}_{内容}.md`
- インデックス: 同ディレクトリの `MEMORY.md` に1行で追記

### メモのフォーマット
```markdown
---
name: kebab-case-slug
description: 一行サマリー
metadata:
  type: project | feedback | reference | user
---

内容

**Why:** なぜこれが重要か
**How to apply:** 次回どう活かすか
```

---

## 有料API・DB書き込みルール

- **DeepL / Claude API** を呼ぶ前にユーザーの許可を取る
- Supabaseへの直接書き込みは行わず、**SQLファイルを生成してユーザーが手動適用**する方式を原則とする
- 有料API使用前に1件だけ書き込みテストをして権限確認する

## APIを使った大量処理の手順（必須）

大量データに対してAPIを実行する場合（翻訳・タグ抽出・バックフィル等）は、必ず以下の順序で進める。

1. **テスト実行（少件数）**
   - まず10件程度の少ない件数で実行し、結果を確認する
   - 例: `npx tsx scripts/enrichCharsV2.ts 10 1 40`（1アニメ・上位10キャラのみ）
   - 出力SQLの内容・翻訳品質・タグ精度をユーザーに確認してもらう

2. **ユーザー確認**
   - テスト結果をユーザーに見せて「本番実行してよいか」承認を取る

3. **本番実行**
   - 承認後に全件実行する
   - 例: `npx tsx scripts/enrichCharsV2.ts 10 60 40`（全60アニメ）

**Why:** 大量のAPI費用を消費した後に品質問題や設定ミスが発覚しても手遅れになるため。テストで問題を早期発見する。
