# 2026-07-12 作業メモ

## 本日の作業サマリー

### 1. キャラ情報拡充の方針決定
- Jikan API（MyAnimeList）を検討 → 説明文が英語のみ・IDマッピングが複雑で断念
- **AniListの未使用フィールドを最大活用する方針に決定**
  - `dateOfBirth`（誕生日）、`bloodType`（血液型）、`image.large`（画像URL）
  - `voiceActors(language: JAPANESE)`（日本語声優名）
  - `description`テキストから身長を正規表現でパース

### 2. DeepL API 導入
- DeepL API Free（月50万文字）に登録してAPIキーを取得
- `.env.local` に `DEEPL_API_KEY` を追加済み
- 翻訳前にキャラ名・アニメタイトルを日本語に事前置換してから翻訳する方式を採用

### 3. ローマ字正規化の追加
- `ou→o`・`uu→u`・`oo→o` の正規化で表記揺れを吸収
  - 例：`Kouhei`（nameMap）vs `Kohei`（description内） → 正規化で一致
- `fetchAndGenerateSQL.ts`・`backfillCharacters.ts` 両方に実装済み

### 4. Supabaseスキーマ拡張
以下の列を `characters` テーブルに追加・適用済み：
```sql
ALTER TABLE characters ADD COLUMN image_url   TEXT;
ALTER TABLE characters ADD COLUMN birthday    TEXT;
ALTER TABLE characters ADD COLUMN blood_type  TEXT;
ALTER TABLE characters ADD COLUMN voice_actor TEXT;
ALTER TABLE characters ADD COLUMN height      INTEGER;
```

### 5. バックフィル実行・適用済み
- `scripts/backfillCharacters.ts` を作成・実行
- 既存キャラ1000件中967件のUPDATE文を生成
- `backfill_characters_part1〜4.sql` をSupabase SQL Editorで実行済み

### 6. CharacterDetailPage.tsx の更新
- 年齢・性別・誕生日・身長・声優を**常に表示**（データなしは「－」表示）
- `birthday`・`voice_actor` の表示を新たに追加

### 7. 翻訳品質の検討
- DeepLの限界：「彼女は〜だ」「彼は〜を持つ」という翻訳調の文体が残る
- **Claude API（Haiku）**で翻訳するとはるかに自然な日本語になることを確認
  - 主語の省略、体言止め、読みやすい文章構成
  - コストは1000件で約50円以下

---

## 次回へのTODO

### 優先度高
- [ ] **翻訳方針の最終決定**
  - A：Claude API（Haiku）で翻訳 → 自然な日本語、要Anthropic APIキー取得、約50円
  - B：DeepLのまま受け入れる → 現状維持

- [ ] **翻訳方針決定後にバックフィルをやり直す**（Claude APIを選んだ場合）
  - `backfillCharacters.ts` の翻訳関数をClaude APIに差し替え
  - 再実行 → SQL適用

### 優先度中
- [ ] **`CharacterDetailPage.tsx` の画像表示を改善**
  - 現状：毎回AniList APIをリクエストして画像取得（遅い）
  - 改善後：DBの `image_url` を直接使用（バックフィル適用済みなので対応可能）

- [ ] **キャラ詳細ページの表示確認**
  - 誕生日・声優・身長・説明文が正しく表示されているか動作確認

### 優先度低
- [ ] **`fix_anilist_ids.sql`（前々回作成）のSupabase適用確認**
  - 韓中原作アニメのキャラ287件がまだ未適用の可能性あり

---

## 変更済みファイル一覧
- `scripts/fetchAndGenerateSQL.ts`（DeepL翻訳・新フィールド・ローマ字正規化対応）
- `scripts/backfillCharacters.ts`（新規作成）
- `src/types/character.ts`（新フィールド追加）
- `src/pages/CharacterDetailPage.tsx`（新フィールド表示・常時表示対応）
- `.env.local`（DEEPL_API_KEY追加）
